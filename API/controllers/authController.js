const signup = (req, res) => {

    res.stasus(202).json({message: "hello"})
}

const login = (req, res) => {

    res.stasus(202).json({message: "hello"})
}

module.exports = {
    signup,
    login
}